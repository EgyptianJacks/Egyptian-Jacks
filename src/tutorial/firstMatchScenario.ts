/**
 * Backward compatibility re-export.
 * The First Match engine has graduated from a tutorial experiment
 * into a first-class product system located in src/firstMatch/.
 */
export * from '../firstMatch';
